export function ProgressBar({done,total,label}:{done:number;total:number;label:string}) { const percent=total ? Math.round(done/total*100):0; return <div className="progress-wrap" aria-label={`${label}: ${done} z ${total}`}><div className="progress-text"><span>{label}</span><strong>{done} / {total}</strong></div><div className="progress" role="progressbar" aria-valuemin={0} aria-valuemax={total} aria-valuenow={done}><span style={{width:`${percent}%`}} /></div></div> }
